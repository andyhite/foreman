{
  "name": "@foreman/omp-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agile SDLC workflow over Linear",
  "omp": {
    "name": "foreman",
    "description": "Agile SDLC workflow over Linear",
    "extensions": ["./dist/extension.js"]
  },
  "scripts": {
    "build": "bun build ./src/extension.ts --outdir ./dist --target node --format esm --external '@oh-my-pi/*'"
  },
  "dependencies": {
    "@foreman/core": "workspace:*"
  }
}
