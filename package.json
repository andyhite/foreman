{
  "name": "foreman",
  "private": true,
  "version": "0.1.0",
  "description": "An omp-native plugin that runs a single-operator agile SDLC over Linear",
  "type": "module",
  "workspaces": ["packages/*"],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "check": "bun run typecheck && bun run test && bun run contract && bun run build",
    "check:ci": "bun run check && bun audit --audit-level=high && bun packages/cli/dist/main.js --help && bash -n scripts/install.sh && bun run schemas && git diff --quiet",
    "contract": "bun run packages/omp-plugin/scripts/check-contract.ts",
    "schemas": "bun run packages/core/scripts/emit-schemas.ts",
    "schema:linear": "bun run packages/core/scripts/validate-linear-schema.ts",
    "setup": "bun run packages/cli/src/main.ts setup --link",
    "test": "bun test",
    "typecheck": "tsc --build --force"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^24.10.1",
    "graphql": "^17.0.2",
    "typescript": "^5.9.3"
  }
}
